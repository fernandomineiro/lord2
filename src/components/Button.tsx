import { IonButton } from "@ionic/react";
import "../theme/global.css";

const Button = () => {
  return (
    <>
      <IonButton color="primary">Name</IonButton>
    </>
  );
};

export default Button;
